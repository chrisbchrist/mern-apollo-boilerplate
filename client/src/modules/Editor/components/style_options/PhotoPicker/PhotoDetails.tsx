import React, { FunctionComponent } from 'react';
import { Modal } from 'antd';

export const PhotoDetails: FunctionComponent<any> = ({ show }) => {

    return (
        <Modal visible={show}>

        </Modal>
    )
};